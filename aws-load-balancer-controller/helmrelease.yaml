apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: aws-load-balancer-controller
  namespace: kube-system
spec:
  interval: 10m
  chart:
    spec:
      chart: aws-load-balancer-controller
      sourceRef:
        kind: HelmRepository
        name: eks
        namespace: flux-system
  values:
    clusterName: anubis-cluster
    region: us-east-1

    serviceAccount:
      create: false
      name: aws-load-balancer-controller

      # Keep going.