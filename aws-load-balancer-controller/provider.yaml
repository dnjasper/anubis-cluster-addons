apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: github-provider
  namespace: flux-system
spec:
  type: githubdispatch
  address: https://github.com/dnjasper/anubis-app-delivery
  secretRef:
    name: flux-system
