apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: anubis-app-delivery-source
  namespace: flux-system
spec:
  interval: 1m
  url: ssh://git@github.com/dnjasper/anubis-app-delivery.git
  ref:
    branch: main
  secretRef:
    name: flux-system
  #  name: flux-git-deploy